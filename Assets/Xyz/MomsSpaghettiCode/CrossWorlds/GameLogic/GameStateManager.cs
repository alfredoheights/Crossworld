using UnityEngine;

namespace Xyz.MomsSpaghettiCode.CrossWorlds.GameLogic
{
    /*
     * 
     */
    public class GameStateManager : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
            
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
